import React, { Children } from 'react';
import video from '../assets/video/'



const VideoBackground = () => {
    return(
        <video src={video} className="video-background">
            {Children}
        </video>
    )
}

export default VideoBackground;